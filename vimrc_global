" This is a common vimrc

" Set nocompatible option, otherwise pathogen does not work.
set nocompatible

" Call Pathogen
call pathogen#runtime_append_all_bundles()
call pathogen#helptags()


" Source user's vimrc
" 'source vimrc_local' does not work, if ~/.vimrc is a symlink to vimrc_global.
runtime vimrc_local
